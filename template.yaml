apiVersion: scaffolder.backstage.io/v1beta3
kind: Template
metadata:
  name: example-system-template
  title: Create System
  description: Create a new System with example documentation
  tags:
    - system
spec:
  owner: group:github
  type: system

  parameters:
    - title: About
      required:
        - name
        - description
        - owner
      properties:
        name:
          title: Name
          type: string
          description: The unique name of this system
          ui:field: EntityNamePicker
        description:
          title: Description
          type: string
          description: Description of this system
        owner:
          title: Owner
          type: string
          description: Owner of this system
          ui:field: OwnerPicker
          ui:options:
            catalogFilter:
              - kind: Group

  steps:
    - id: fetch
      name: Fetch Skeleton
      action: fetch:template
      input:
        url: ./skeleton
        copyWithoutRender:
          - .github/workflows/*
        values:
          name: ${{ parameters.name }}
          description: ${{ parameters.description }}
          owner: ${{ parameters.owner }}

    - id: publish
      name: Publish
      action: publish:github
      input:
        repoUrl: github.com?owner=spotify-portal-public-demo&repo=${{ parameters.name }}-system
        description: ${{ parameters.description }}
        repoVisibility: public
        defaultBranch: main
        protectEnforceAdmins: false

    - id: register
      name: Register
      action: catalog:register
      input:
        repoContentsUrl: ${{ steps.publish.output.repoContentsUrl }}
        catalogInfoPath: /catalog-info.yaml

  output:
    links:
      - title: Repository
        url: ${{ steps.publish.output.remoteUrl }}
      - title: Open in catalog
        icon: catalog
        entityRef: ${{ steps.register.output.entityRef }}
